import 'package:flutter/material.dart';

class CloudGridView extends StatefulWidget {
  const CloudGridView({super.key});

  @override
  State<CloudGridView> createState() => _CloudGridViewState();
}

class _CloudGridViewState extends State<CloudGridView> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Cloud Grid view he?"),
    );
  }
}
